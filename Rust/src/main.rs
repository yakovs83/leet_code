//use leet_code::regex_matching::*;

fn main() {
  // let s = "aab".to_owned();
  // let p = "c*a*b".to_owned();
  // let res = is_match(s.clone(), p.clone());
  // println!(
  //   "\nString is {:?}\nPattern is {:?}\nResult is {:?}",
  //   s, p, res
  // );
}
